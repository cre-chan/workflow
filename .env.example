# Copy to .env and replace with an absolute path. Never add auth.json here.
CODEX_AUTH_DIR=/absolute/path/to/.codex-moth-watcher-runner
# Dedicated fine-grained PAT file, mode 0600. Do not store the token in .env.
ADMIN_GITHUB_TOKEN_FILE=/absolute/path/to/moth-watcher-admin-token
